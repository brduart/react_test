import { createContext } from "react";

//VALOR INICIAL
export const CountInitialData = 10;

//CRIAÇÃO DO CONTEXTO
export const CountContext = createContext(CountInitialData); //()VALOR PADRÃO, NUMBER, ARRAY ETC...
